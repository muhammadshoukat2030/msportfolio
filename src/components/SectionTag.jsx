import React from 'react';

const SectionTag = ({ text }) => (
  <div className="inline-block px-3 py-3 rounded-full border border-purple-500/30 bg-purple-500/10 text-purple-400 text-[10px] font-bold uppercase tracking-widest mb-4">
    {text}
  </div>
);

export default SectionTag;